import Phaser from "phaser";

/** Stub — populated in a later slice (results / scoring summary). */
export class ResultScene extends Phaser.Scene {
  constructor() {
    super("ResultScene");
  }

  create(): void {
    // Intentionally empty in V1.
  }
}
